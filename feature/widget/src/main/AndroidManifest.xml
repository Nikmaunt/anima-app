<?xml version="1.0" encoding="utf-8"?>
<!--
  ADR-007: the widget receiver is one of the two sanctioned background
  entities besides the notification listener (the other being WorkManager's
  charge-edge one-shots). It renders a static bitmap; no loop, no inference,
  no network.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name="app.anima.feature.widget.AnimaWidgetReceiver"
            android:exported="true"
            android:label="Anima">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/anima_widget_info" />
        </receiver>
    </application>
</manifest>
