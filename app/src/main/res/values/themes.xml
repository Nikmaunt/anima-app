<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pre-Compose window theme; the real design system lives in :core:ui. -->
    <style name="Theme.Anima" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/anima_window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
