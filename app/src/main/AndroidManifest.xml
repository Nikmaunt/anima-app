<?xml version="1.0" encoding="utf-8"?>
<!--
  Permission budget (ADR-004 as amended by ADR-005), in full:
  - ACCESS_NETWORK_STATE, VIBRATE: normal install-time permissions, no dialogs.
  - INTERNET is declared by exactly one module manifest —
    :core:model-delivery — for exactly one flow: the user-initiated download
    of the mind's model file. No other module may declare it or touch network
    APIs in source; NetworkIsolationTest fails the build otherwise (and fails
    loudly if the merged manifest is missing, closing the v0.1 vacuous-pass
    gap found by the audit).
  - Notification Access is a special access granted on the system screen,
    strictly opt-in from an in-app explainer; the listener service lives in
    :feature:notifications with the system-held bind permission.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".AnimaApp"
        android:label="@string/app_name"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:supportsRtl="true"
        android:theme="@style/Theme.Anima">

        <!-- Strip DataTransport telemetry components (see note above). -->
        <service
            android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.JobInfoSchedulerService"
            tools:node="remove" />
        <receiver
            android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.AlarmManagerSchedulerBroadcastReceiver"
            tools:node="remove" />
        <service
            android:name="com.google.android.datatransport.runtime.backends.TransportBackendDiscovery"
            tools:node="remove" />

        <!-- Soul export: one readable file handed to the share sheet. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
